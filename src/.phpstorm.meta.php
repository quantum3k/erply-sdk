<?php

namespace PHPSTORM_META {

    // Этот код определяет, что метод make() принимает два аргумента, первый из которых является классом,
    // а второй - любым типом. А возвращаемое значение определяется с помощью переменной $class,
    // которая ссылается на класс, переданный в первом аргументе метода.

    override(\quantum3k\ErplySDK\ErplyAPI::make(0, ''), typeFromSignature("({\$class}, {*}) -> {$class}"));

}
